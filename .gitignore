# -----------------------------
# Editors & OS junk
# -----------------------------
*~
*.swp
*.swo
.DS_Store
Thumbs.db

# -----------------------------
# Node/Bun / JS tool caches
# -----------------------------
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage/
.nyc_output/
.eslintcache
*.tsbuildinfo

# -----------------------------
# Build artifacts
# -----------------------------
build/
dist/
workspace/
bin/

# -----------------------------
# Misc data/backup artifacts
# -----------------------------
*.dat
*.bin
*.bak
*.bak2
*.bak3
*.bak.*

# -----------------------------
# Project-specific one-offs
# -----------------------------
typecheck.txt
openai-patch.sh
patch-debug.sh
patch.sh
patch.py
cchat_client
cchat_server
transcript.txt

# If you still generate top-level logs/, ignore it
logs/

# =============================
# .org folder policy
# =============================
# Track the directory itself
!/.org/

# Ignore everything inside by default
/.org/*

# Keep launcher code and any static assets you want to ship
!/.org/launcher/
!/.org/launcher/**
!/.org/bin

# Optionally keep docs/config in .org
!/.org/*.md
!/.org/*.json
!/.org/*.example
!/.org/*.sample

# If you keep helper scripts in .org, allow them:
!/.org/*.sh

# Ignore runtime artifacts under .org
/.org/logs/
/.org/runs/
/.org/tmp/
/.org/tmp/
/.org/sandboxes/
/.org/**/tmux-logs/
/.org/last.log
/.org/last-session.patch

# --- org runtime artifacts ---
.org/logs/
.org/logs/**           # rotating run logs (and last.log symlink)
.org/runs/             # per-run artifacts / patches (if you keep them here)
.org/sandboxes/        # sandbox/cache, if used
.org/tmux-inner.sh    # generated inner script used by tmux

# tmux debug logs (in case they ever land in repo root)
tmux-*.log


